# worker_house.gd
# 집 스크립트 - 집 안과 밖의 네비게이션 연결

extends Node2D

func _ready() -> void:
	# 그룹에 추가하여 쉽게 찾을 수 있도록
	add_to_group("worker_house")
	
	# 🆕 집 안과 밖을 연결하는 네비게이션 링크 생성
	# NavigationLink2D는 문제를 일으킬 수 있으므로 비활성화
	# 대신 네비게이션 메쉬가 자연스럽게 연결되도록 함
	# call_deferred("_setup_navigation_link")
	pass

func _setup_navigation_link() -> void:
	# 🚫 NavigationLink2D 비활성화
	# 문제: 링크가 이상한 중간 경로를 만들어냄
	# 해결: 네비게이션 메쉬를 하나로 통합하여 자연스럽게 연결
	pass
	
	# # 문 위치 찾기 (집의 입구)
	# # 예: 집 중앙 하단을 문으로 가정
	# var door_position = Vector2(0, 100)  # 집 로컬 좌표
	# 
	# # NavigationLink2D 생성
	# var nav_link = NavigationLink2D.new()
	# add_child(nav_link)
	# 
	# # 집 안 지점 (Floor 타일 위로 확실하게)
	# nav_link.start_position = Vector2(0, 0)
	# 
	# # 집 밖 지점 (Grass/Dirt 타일 위로 확실하게)
	# # WorkerHouse의 scale이 1.5이므로, 로컬 좌표 200은 월드 좌표 300 정도가 됨.
	# # 너무 멀면 연결이 안 될 수 있으므로 적절히 조정 (예: 100 -> 150)
	# nav_link.end_position = Vector2(0, 120)
	# 
	# nav_link.bidirectional = true
	# nav_link.enabled = true
	# 
	# print("🔗 네비게이션 링크 생성: 집 안 ↔ 밖")

# 이전 장애물 생성 코드는 제거 (작동 안 함)
